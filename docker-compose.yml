version: "3.9"
services:
  penx-sse-server:
    container_name: penx-sse-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '4000:4000'
    environment:
      - NODE_ENV=production
