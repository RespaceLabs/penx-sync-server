version: "3.9"
services:
  penx-sync-server-local:
    container_name: penx-sync-server-local
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4002:4000"
    environment:
      - NODE_ENV=local
